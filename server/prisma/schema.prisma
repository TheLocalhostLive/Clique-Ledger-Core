generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Cliques {
  id Int @id @default(autoincrement())
  name String
  fund Int
  spend Int
  isActive Boolean
}